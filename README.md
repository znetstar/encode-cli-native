This plugin enables the use of `EncodeToolsNative` in `encode-cli`. A better explanation of what this is supposed to
do can be found in the [`encode-cli` README](https://github.com/znetstar/encode-cli).
